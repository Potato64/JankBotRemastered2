package org.firstinspires.ftc.teamcode;

// an interface, for ease of changing driver controls. because we might have needed that
public interface DriveOperator
{
    public double speed();

    public double rotSpeed();
}
